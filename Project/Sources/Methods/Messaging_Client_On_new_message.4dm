//%attributes = {"invisible":true}
#DECLARE($conversationId : Integer; $messageId : Integer; $senderId : Integer)

// Client-side notification hook. Replace or extend this method to refresh open forms.
BEEP
